# Domain auditors (gcp_audit/domains/) shell out to the `gcloud` CLI
# directly via subprocess, so no google-cloud-* client libraries are
# required. Install and authenticate the Google Cloud SDK separately —
# see README Prerequisites.

# Optional: enables real Gemini-based cross-domain risk synthesis
# (skills/risk_synthesis.md) when GEMINI_API_KEY is set. Without it (or
# without this package installed), gcp_audit/synthesis.py falls back to a
# deterministic rule-based synthesiser, so the harness is fully runnable
# without this dependency.
google-generativeai>=0.8.0

# Utilities
python-dotenv>=1.0.0
